[project]
name = "deeplearning-pytorch-textbook"
version = "0.1.0"
description = "uv로 구성해본 pytorch"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.6.0",
    "torchvision>=0.21.0",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
